services:
  - type: web
    name: agnes-ia
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: AGNES_API_KEY
        sync: false
      - key: PORT
        value: 10000
    healthCheckPath: /
